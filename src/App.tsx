import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';
import './App.scss';

const App = () => (
  <div className="app">
    <Router>
      <Routes />
    </Router>
  </div>
);

export default App;
